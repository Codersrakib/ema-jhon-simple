import React from 'react';

const NotFound = () => {
    return (
        <div>
            <h1 style={{color:'red',textAlign:'center',background:'yellow'}}>Warning...</h1>
            <h6 style={{color:'red',textAlign:'center',}}>404 Error page not found.</h6>
        </div>
    );
};

export default NotFound;